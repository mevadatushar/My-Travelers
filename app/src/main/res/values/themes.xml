<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyTravelers" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="MyRatingBar" parent="Widget.AppCompat.RatingBar">
        <!--        non selected star-->
        <item name="colorControlNormal">#4D000000</item>

        <!--        selected star-->
        <item name="colorControlActivated">@color/light_green</item>
    </style>

    <style name="Theme.MyTravelers" parent="Base.Theme.MyTravelers" />
</resources>